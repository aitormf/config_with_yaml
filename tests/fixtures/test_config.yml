database:
  host: localhost
  port: 5432
  name: myapp
  settings:
    timeout: 30
    max_connections: 100

features:
  enabled: true
  debug: false

simple_key: value

nested:
  level1:
    level2:
      level3: deep_value
